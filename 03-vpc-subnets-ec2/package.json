{
  "name": "03-vpc-subnets-ec2",
  "main": "index.ts",
  "scripts": {
    "keygen": "mkdir -p keys && ssh-keygen -t rsa -f keys/rsa -m PEM"
  },
  "devDependencies": {
    "@types/node": "^18"
  },
  "dependencies": {
    "@pulumi/aws": "^6.32.0",
    "@pulumi/pulumi": "^3.113.0",
    "typescript": "^5.0.0"
  }
}
